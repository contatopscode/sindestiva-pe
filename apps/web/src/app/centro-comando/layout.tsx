// =============================================================================
// SINDESTIVA-PE · Layout local de /centro-comando
// Apenas o conteúdo; Sidebar/Header vêm do layout raiz.
// =============================================================================

import type { ReactNode } from "react";

export default function CentroComandoLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
